//Format document with Shift + Alt + F

//Start here

const typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    backSpeed: 85,
    typeSpeed: 60,
    loop: true,
    backDelay: 500,
    startDelay: 1000,
    smartBackspace: true
  });